import './Answers.css'
const Answers = ({props}) => {
  return (
    <div>
      <div className='Answers'>
      <div>
        {/* {props.map((item) => (
          <div key={item.id}>
            <div className='d-flex'>
              <img src={item.image} alt="img" />
              <div>
                <h6>{item.name}</h6>
              </div>
            </div>
          </div>
        ))} */}
      </div>
     </div>
    </div>
  )
}

export default Answers
